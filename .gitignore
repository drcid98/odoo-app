# Plain secrets - DO NOT commit
*-secret.yaml
!*-secret.example.yaml

# Sealed secrets ARE safe to commit (they're encrypted)
# gcs-s3-sealedsecret.yaml - OK to commit

# Environment files with secrets
.env
*.env.local
