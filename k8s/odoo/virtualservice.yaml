apiVersion: gateway.solo.io/v1
kind: VirtualService
metadata:
  name: odoo-app
  namespace: gloo-system
spec:
  virtualHost:
    domains:
      - odoo-app.test.local
    routes:
      - matchers:
          - prefix: /
        routeAction:
          single:
            upstream:
              name: odoo-odoo-80
              namespace: gloo-system
